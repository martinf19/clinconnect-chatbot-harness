package com.clinconnect.chatbot.time;

/** Canonical time_expression.kind enum (docs/08-API-CONTRACTS.md). */
public enum TimeExpressionKind {
    CURRENT,
    TODAY,
    TONIGHT,
    TOMORROW,
    WEEKEND,
    SPECIFIC_DATE
}
