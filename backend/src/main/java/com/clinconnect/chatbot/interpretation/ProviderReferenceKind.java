package com.clinconnect.chatbot.interpretation;

/** Canonical provider_reference.kind (docs/05-INTENT-CATALOG.md). */
public enum ProviderReferenceKind {
    EXPLICIT_TEXT,
    LAST_RESULT_PROVIDER
}
