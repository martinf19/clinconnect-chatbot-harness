package com.clinconnect.chatbot.session;

/** Canonical clarification reason (FR-012). */
public enum ClarificationReason {
    MISSING_PARAMETER,
    AMBIGUOUS_ENTITY,
    AMBIGUOUS_RESULT,
    LANGUAGE_UNCERTAIN
}
